# Telemetry Tracker Claude Instructions

Use the workflows in `skills/telemetry-coach/SKILL.md` and `skills/setup-proposal/SKILL.md`.

Call the local `telemetry-tracker` JSON CLI for all telemetry evidence. Keep advice evidence-led, preserve uncertainty, and never export or apply LMU setup changes automatically.
